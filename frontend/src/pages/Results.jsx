import PlaceholderPage from '../components/PlaceholderPage';

/**
 * Results page component
 * 
 * @returns {JSX.Element} - Results page
 */
const Results = () => {
    return <PlaceholderPage title="Results" />;
};

export default Results;
